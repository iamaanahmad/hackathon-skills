# Demo Engineering

Design for deterministic success.

Always define:
- primary flow
- fallback flow
- mock/offline data
- failure points + recoveries
- reset steps between runs

Never hinge the entire demo on a single unstable external API call.
